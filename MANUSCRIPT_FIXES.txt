MANUSCRIPT FIXES — Overleaf/MA-LB-PQ-VDSE.tex
=============================================
Written 2026-09-06. UPDATED 2026-09-07: items 1-8 have now ALL BEEN APPLIED to
the .tex on the author's instruction ("Paste MANUSCRIPT_FIXES.txt into Overleaf,
you do it"). This file is kept as the record of what changed and why. Anything
added below the APPLIED marker is still outstanding.

Each item gives the location, what was wrong, and the text that replaced it.

Two items (A1, A2) were applied earlier on 2026-09-06 before this rule was set
and are listed at the bottom as ALREADY APPLIED, so the record is complete.


--------------------------------------------------------------------------
1. §V Exp. 2 — Scheme [41]'s upper points are EXTRAPOLATED, not measured
--------------------------------------------------------------------------
WHERE   §V, Experiment 2, and the caption of fig:exp2.

WHY     Scheme [41] (Thingom) is measured only at N=10^4. The 50k / 100k /
        500k / 1M points are produced by scaling that anchor linearly
        (infra/extrapolate_points.py); they carry n_runs=1 and a blank CI, and
        generate_plots.py draws them as HOLLOW markers. A hollow marker tells
        a reader something differs, not what.

        Its search is O(N) pairings with no early termination, as published.
        One run at N=10^6 is ~20.5 h extrapolated from its own measured 10^4
        anchor (737.7 s/run), so 10 runs + 5 warm-ups is ~307 h for that point
        alone.

ADD     to the fig:exp2 caption:
        "Scheme~\cite{ref41}'s points above $N=10^4$ are extrapolated linearly
        from its measured $N=10^4$ anchor and are drawn with hollow markers;
        its search is $O(N)$ pairings with no early termination, which places
        a full sweep beyond the per-track compute budget."

DECIDED 2026-09-07: the extrapolation STAYS. Measuring those points was
        started and then stopped on the author's instruction -- "search single
        or 10k, then multiply into a straight line, do not run the whole
        million". The arithmetic behind that call, from Scheme [41]'s own
        banked raw_runs.csv (740.1 s per run at N=10^4, 10 runs = 123 min):

            N        per run     per point (10 runs + 5 warm-ups)
            50,000     1.03 h        15.4 h
            100,000    2.05 h        30.8 h
            500,000   10.2  h       154   h
            1,000,000 20.5  h       307   h   (~12.8 days)

        So the four missing points are ~508 instance-hours for one baseline's
        one experiment. Every other scheme -- the proposed one, [30], [35] and
        [54] -- already MEASURES N=10^6 at n=10, so [41] is the only curve
        carrying extrapolated points and the disclosure is confined to it.


--------------------------------------------------------------------------
2. §V Exp. 7-8 — state the workload, which was wrong until 2026-09-06
--------------------------------------------------------------------------
WHERE   §V, Experiments 7 and 8.

WHY     The trace built one keyword per query while §V and README §6 both fix
        q=5. Fixed and re-measured on 2026-09-06; every Exp. 7-8 number in the
        paper must come from the new run, not the banked q=1 one.

ADD     "Each replayed request carries the same $q=5$ conjunctive query the
        rest of Section~V uses."

NOTE    The re-run STRENGTHENED the result: AASS is now first at every
        concurrency (2255 / 3275 / 3622 queries/s at 100 / 1000 / 10000),
        where at q=1 the ordering moved around below concurrency 1000.


--------------------------------------------------------------------------
3. §V Exp. 8 — least_loaded wins the spread metric, and that is fine
--------------------------------------------------------------------------
WHERE   §V, Experiment 8.

WHY     `least_loaded` takes the lowest utilization standard deviation
        (0.0437 against AASS's 0.0587 at concurrency 10000). It minimises
        queue length by construction, so it wins that metric by construction.
        AASS trades spread for authorization locality. Saying AASS wins every
        metric invites a reviewer to check, and the check fails.

ADD     "Least-loaded scheduling attains the lowest utilization spread, which
        follows from its rule; AASS trades a small amount of spread for
        authorization locality and leads on throughput at every concurrency."


--------------------------------------------------------------------------
4. §V Exp. 6 — Full-State's cost is a LOWER BOUND
--------------------------------------------------------------------------
WHERE   §V, Experiment 6.

WHY     The Full-State arm redistributes authorization state to every FSN, but
        models each authority's commitment RECOMPUTATION as a digest rather
        than a Merkle rebuild. The benchmark world holds authority commitments
        as fixed values with no state behind them, and giving them one means
        choosing a state size the paper does not publish.

ADD     "The Full-State arm's authorization-state reconstruction is modelled
        by its commitment digest rather than a full re-derivation, so its
        reported cost is a lower bound."


--------------------------------------------------------------------------
5. §V Exp. 6 — the selective saving is in DELIVERY VOLUME
--------------------------------------------------------------------------
WHERE   §V, Experiment 6, and the fig:exp6 caption.

WHY     DIAS and Incremental-All evolve identical policy sets and differ only
        in fan-out. In an in-process harness a delivery is a function call, so
        the selective saving shows in bytes (a clean 4x) and only 2-11% in
        latency. A reader given only the latency panel sees two curves nearly
        on top of each other.

ADD     "Because the harness models no network, Incremental-All's unnecessary
        propagation appears chiefly in transferred data rather than in
        sender-side latency."


--------------------------------------------------------------------------
6. §V — the policy-state-aware track is NOT a second scheme
--------------------------------------------------------------------------
WHERE   §V, Experimental Setup.

WHY     Five psa_exp* directories are built on in-process data and are marked
        non-reportable; only psa Exp. 2, 7 and 8 read the frozen corpus. If
        any psa figure is used, §V must say which construction produced it.

ADD     (only if psa figures are included) "Measurements of the
        policy-state-aware construction of Section~IV are reported separately;
        those of Experiments 1, 3, 4, 5 and 6 are obtained on in-process
        fixtures and are not comparable with the corpus-backed results."


--------------------------------------------------------------------------
7. tab:cost — Auth. Sync row has no chain term, and that is deliberate
--------------------------------------------------------------------------
WHERE   tab:cost, the authorization-synchronization row; §V Exp. 6.

WHY     Exp. 6's measurement ends when every affected FSN reports the new
        state. Phase VII's anchoring step is OUTSIDE that boundary and is
        reported as a per-update constant, the same treatment Exp. 1 gives
        ML-KEM. The row already omits a chain term; the text should say why.

ADD     "Blockchain anchoring is excluded from the synchronization boundary
        and reported separately, as it is invariant across the compared
        configurations."


--------------------------------------------------------------------------
8. §V Exp. 4 — Scheme [54] is IN, and DECIDE-1 says so
--------------------------------------------------------------------------
WHERE   §V, Experiment 4.
STATUS  VERIFY ONLY. §V now names Schemes [30], [35], [54], which is exactly
        the measured roster, and states why [54] is absent from panel (b).
        Audited 2026-09-06: all five cross-scheme experiments cite exactly what
        they measure. No edit needed unless the text changes again.


--------------------------------------------------------------------------
ALREADY APPLIED on 2026-09-06 (before the "leave the .tex alone" rule)
--------------------------------------------------------------------------
A1  §V Exp. 1 baselines: "XB-Muse~\cite{ref36} ... Zhuang~\cite{ref52}" ->
    "Ge \textit{et al}.~\cite{ref30} ... Perera and Fugkeaw~\cite{ref54}".
    Ref[36] was dropped (needs SGX), Ref[52] replaced by Ref[54]; neither has
    an implementation. NOTE: this edit first cited \cite{ref55} by mistake --
    yue_ge is Scheme [30], not [55] -- and was corrected the same day.

A2  §V Exp. 4: "incurs a moderate per-result verification cost" -> "incurs the
    highest per-result verification cost of the four compared schemes; the
    axis is logarithmic for that reason", plus restoring the sentence
    explaining why Scheme [54] is omitted from Fig. 4(b). Both had been fixed
    in d558d63 and reverted by an Overleaf paste-back (divergence D13).


--------------------------------------------------------------------------
STILL OPEN, needs the author's decision (not a wording fix)
--------------------------------------------------------------------------
D1  DECIDE-2: do the baselines report mean or median? Guo's Exp. 2 at N=10^6
    spans 0.33-1408.65 ms and its mean is 16.9x its median. Whichever is
    chosen, §V must state it and the figures must follow.

D2  FIX-2: "manuscript claims contradicted by measurement" is on the task
    board with no list attached. It needs the specific sentences.



==========================================================================
APPLIED 2026-09-07 — items 1-8, plus FIX-2 and DECIDE-2
==========================================================================
Items 1, 2, 3, 4, 5, 6 and 7 were applied as written above. Item 8 was
verify-only and needed no edit. In addition:

DECIDE-2  RESOLVED: mean. No edit was needed -- §V already reads "the reported
          results represent the mean with 95\% confidence intervals", and every
          results.csv carries means. The decision confirms the existing text
          rather than changing it. Guo's Exp. 2 at N=10^6 has a mean 16.9x its
          median, so §V should be prepared to defend the spread if asked; the
          data supporting it is in that experiment's raw_runs.csv.

FIX-2     RESOLVED as three concrete defects, found by auditing §V's setup
          against run_meta.json and the bibliography rather than by guessing:

          (a) HOST WAS WRONG. §V said "c6i.xlarge ... 4 vCPUs and 8 GB RAM".
              Every measurement ran on m6i.xlarge with 16 GB -- README §1 pins
              it and AWS confirms the live instance type. The change was made
              on 2026-08-03 because Scheme [52] allocates a 381 MB lattice
              trapdoor per attribute and 8 GiB ran near saturation, which
              contaminates latency with GC and page-cache effects. README §14
              item 3 had flagged this and it had not been carried over.
              -> now reads "m6i.xlarge ... 4 vCPUs and 16 GB RAM".

          (b) BROKEN CITATION. \cite{refSynthea} was used in §V's dataset
              paragraph but no \bibitem{refSynthea} existed, so it would
              render as a bare "?" in the PDF. Added at the END of the
              bibliography, deliberately: \begin{thebibliography} numbers
              POSITIONALLY, so inserting anywhere earlier would shift every
              later entry and silently break the [30], [35], [41], [54]
              references the text depends on. Verified after the edit that
              every refN is still the Nth entry.

          (c) FOUR UNCITED ENTRIES remain: ref34, ref44, ref49 and ref56
              (MIMIC-IV). They are NOT removed, for the same positional
              reason -- deleting ref34 would renumber Scheme [35] to [34] and
              every later citation with it. ref56 is the last trace of the
              MIMIC-IV corpus, dropped on 2026-08-03 in favour of Synthea.
              Leaving an uncited entry is harmless; renumbering the
              bibliography is not.

STILL OUTSTANDING for the author
--------------------------------
README §16 change-log line for 2026-09-06/07. README §13 reserves that file
for the author, and this session did not touch it. It has been a
results-affecting two days: the conjunctive-query defect, the q=1 -> q=5
workload fix, and the psa track's first corpus-backed results all belong in it.


==========================================================================
NEW 2026-09-07 (after the Fabric run) — one addition and one correction
==========================================================================
9.  §V Exp. 4 — the policy-state-aware commitment costs nothing measurable
    -------------------------------------------------------------------
    NOT YET APPLIED. Needs the author's decision on whether Exp. 4 reports the
    construction at all.

    psa Exp. 4 has now been measured against the SAME real Hyperledger Fabric
    network as the cross-scheme Exp. 4, so the two are finally on one axis:

        r        PSA      Option D    ratio
        10      19.03 ms   18.72 ms   1.017x
        100    167.85 ms  171.79 ms   0.977x
        1000  1668.56 ms 1675.34 ms   0.996x

    Within 2% at every point, and BELOW Option D at four of five. D3's richer
    commitment -- CID and AuthState folded in, where Option D binds AuthRoot_DO
    and leaves the ciphertext reference outside -- adds no measurable
    verification cost, because the per-record chain round-trip dominates
    everything the construction changed.

    SUGGESTED "Binding the ciphertext reference and the governing authorities'
              state into the record commitment leaves per-result verification
              cost unchanged within measurement error, as the cost is dominated
              by the per-record ledger lookup."

10. §V setup — Exps. 7 AND 8 are excluded from the psa track (CORRECTION)
    --------------------------------------------------------------------
    ALREADY APPLIED. An edit earlier on 2026-09-07 said the construction's
    throughput WAS obtained comparably; that was written before psa Exp. 7 had
    been re-run with a query that matches. It now lands within noise across all
    four variants (2453.6 / 2516.2 / 2493.9 / 2515.2 q/s at concurrency 10,000,
    rank order shuffling between points), for the same reason Exp. 8 does: a
    request is dispatched to ONE node and that node evaluates only the query
    groups whose domain it serves, so per-request cost is set by the
    policy/domain overlap rather than by the scheduling rule. §V now excludes
    both experiments.
